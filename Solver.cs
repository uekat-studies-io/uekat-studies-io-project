static class Solver
{
    public static double Solve(Map map)
    {
        var totalDistance = 0;
        while (map.Stores.Any(store => store.ProductNeeds.Values.Any(need => need != 0)) {

        }
        return totalDistance;
    }
}